name: shipping_order_repository
description: The Repo that handles shipping_order
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.1

#Repository
  driver_repository:
    path: ../driver_repository
  user_repository:
    path: ../user_repository
  transaction_repository:
    path: ../transaction_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true